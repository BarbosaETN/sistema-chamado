const STATUS_CADASTRO = Object.freeze({
  PENDENTE: 'Pendente',
  APROVADO: 'Aprovado',
  REJEITADO: 'Rejeitado',
});

export const STATUS_CADASTRO_VALUES = Object.freeze(Object.values(STATUS_CADASTRO));

export default STATUS_CADASTRO;